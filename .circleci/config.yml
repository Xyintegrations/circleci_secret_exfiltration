version: 2.1

jobs:
  say-hello:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: "Say hello"
          command: "export SECRET=$(echo $MY_USER | base64); echo Hello, $SECRET!"

workflows:
  say-hello-workflow:
    jobs:
      - say-hello
